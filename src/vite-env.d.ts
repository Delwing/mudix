/// <reference types="vite/client" />

/** Injected by Vite `define` from package.json — see buildInfo.ts. */
declare const __APP_VERSION__: string;
/** Injected by Vite `define`: short HEAD hash, or '' when git is unavailable. */
declare const __GIT_COMMIT__: string;
